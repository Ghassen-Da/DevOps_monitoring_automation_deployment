apiVersion: v1
kind: Service
metadata:
  name: service-ecommerce
  annotations:
    prometheus/scrape: "true"
    prometheus/path: "/metrics"
    prometheus/port: "9464"
spec:
  selector:
    app: ecommerce
  ports:
    - name: "main"
      port: 8080
      targetPort: 8080
    - name: "prometheus"
      port: 9464
      targetPort: 9464
